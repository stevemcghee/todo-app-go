apiVersion: batch/v1
kind: Job
metadata:
  name: db-init
  labels:
    app: todo-app-go
spec:
  template:
    metadata:
      labels:
        app: todo-app-go-db-init
    spec:
      shareProcessNamespace: true
      restartPolicy: OnFailure
      containers:
      - name: db-init
        image: postgres:15-alpine
        command:
          - /bin/sh
          - -c
          - |
            echo "Waiting for database to be ready..."
            until pg_isready -h 127.0.0.1 -p 5432 -U ${DB_USER}; do
              echo "Database not ready, waiting..."
              sleep 2
            done
            echo "Database ready, running init.sql..."
            psql -h 127.0.0.1 -p 5432 -U ${DB_USER} -d ${DB_NAME} << 'EOF'
            CREATE TABLE IF NOT EXISTS todos (
                id SERIAL PRIMARY KEY,
                task TEXT NOT NULL,
                completed BOOLEAN DEFAULT FALSE
            );
            EOF
            echo "Database initialization complete!"
            echo "Terminating sidecar..."
            pkill cloud_sql_proxy || true
        env:
        - name: DB_HOST
          value: "127.0.0.1"
        - name: DB_PORT
          value: "5432"
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: username
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: password
        - name: DB_NAME
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: dbname
        - name: PGPASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: password
      - name: cloudsql-proxy
        image: gcr.io/cloudsql-docker/gce-proxy:1.17
        command:
          - "/cloud_sql_proxy"
          - "-instances=smcghee-todo-p15n-38a6:us-central1:todo-app-db-instance=tcp:5432"
        securityContext:
          runAsNonRoot: true
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
